#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"$ROOT_DIR/scripts/kafka-smoke" plain
"$ROOT_DIR/scripts/kafka-smoke" scram
"$ROOT_DIR/scripts/kafka-test" plain
